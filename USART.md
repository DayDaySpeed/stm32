# USART 从入门到电路实现（STM32 视角）

这份文档把前面讨论的内容系统整理为一条完整链路：

- 先懂 `TXE/TC/DR` 这些常见缩写
- 再懂发送与接收时序（为什么是 1.5 bit）
- 再懂 `BRR/OVER8` 如何决定节拍与抗干扰
- 最后落到“`Mantissa + Fraction` 的电路怎么实现”

---

## 1. 先建立全局图：USART 发送到底经过哪些部件

发送路径（逻辑上）：

`CPU -> DR(数据寄存器) -> 发送移位寄存器 -> TX 引脚`

接收路径（逻辑上）：

`RX 引脚 -> 接收移位寄存器 -> DR(数据寄存器) -> CPU`

时钟/节拍路径：

`系统时钟 fCK -> 波特率发生器(BRR) -> 采样 tick -> 状态机/移位寄存器`

---

## 2. 常用缩写到底是什么意思

### 2.1 DR（Data Register）

- 含义：数据寄存器
- 发送时：CPU 把要发的字节写到 `DR`
- 接收时：CPU 从 `DR` 读收到的字节

它是 CPU 与串口硬件之间的“数据中转站”。

### 2.2 TXE（Transmit Data Register Empty）

- 含义：发送数据寄存器空
- `TXE=1`：`DR` 空，可以写下一个字节
- `TXE=0`：`DR` 还没空，暂时不能写

本质问题：**现在能不能继续塞下一个字节？**

### 2.3 TC（Transmission Complete）

- 含义：发送完成
- `TC=1`：最后一个字节连停止位都已经发完
- `TC=0`：线上的比特还在发送中

本质问题：**是不是“真的全发完”了？**

### 2.4 TXE 和 TC 的关键区别

- `TXE=1` 只代表 `DR` 空了（可继续写）
- `TC=1` 代表 `DR` 空 + 移位寄存器空 + TX 线也结束

所以最后一个字节发送后，收尾要等 `TC`，而不是只看 `TXE`。

---

## 3. 轮询发送流程（以发送 `"OK\n"` 为例）

典型轮询代码：

```c
while (!(USARTx->SR & USART_SR_TXE)) { }
USARTx->DR = 'O';

while (!(USARTx->SR & USART_SR_TXE)) { }
USARTx->DR = 'K';

while (!(USARTx->SR & USART_SR_TXE)) { }
USARTx->DR = '\n';

// 收尾：等待最后停止位也发完
while (!(USARTx->SR & USART_SR_TC)) { }
```

时序逻辑：

1. 等 `TXE=1`，写 `DR`
2. 写入后 `TXE=0`
3. 硬件把 `DR` 搬到发送移位寄存器，`DR` 又空，`TXE=1`
4. 重复直到最后一个字节
5. 最后必须等 `TC=1`

---

## 4. 移位寄存器到底是什么

移位寄存器（Shift Register）本质是“逐 bit 推送器”：

- 并行装入一个字节
- 按波特率时钟每次移一位
- 把当前最低位（LSB）送到 TX 线上

UART 常见帧格式（8N1）在线上的顺序是：

`空闲(1) -> 起始位(0) -> 数据位0..7(LSB first) -> 停止位(1)`

关键分工：

- `DR`：缓存区（CPU 可写）
- 移位寄存器：真正对外逐位发送

---

## 5. 为什么常见是 LSB first（低位先发）

结论：不是物理“必须”，而是工程上长期约定且实现简单。

主要原因：

1. 右移发送电路简单（历史上硬件资源紧）
2. 接收端从 bit0 开始组装数据很自然
3. 软件位操作实现简单（`data >>= 1`）
4. 早期标准形成后沿用至今

---

## 6. 波特率是怎么控制发送节奏的

核心器件：波特率发生器（Baud Rate Generator），由 `BRR` 配置。

在 `OVER8=0`（16 倍过采样）时：

`USARTDIV = fCK / (16 * Baud)`

意义：

- 先由系统时钟分频出“采样 tick”
- 每 `16` 个 tick 构成 `1 bit` 时间
- 移位寄存器按该节拍发送

---

## 7. 接收端为什么是“1.5 bit 处首次采样”

这是 UART 接收对齐的核心。

### 7.1 两个时间单位先分开

- `1 bit`：串口符号时间
- `tick`：过采样内部小节拍
  - `OVER8=0` 时：`1 bit = 16 tick`
  - `OVER8=1` 时：`1 bit = 8 tick`

### 7.2 为什么是 1.5 bit

接收端检测到起始位下降沿（`1 -> 0`）后开始计时：

- 经过 `1.5 bit` 采样，正好落在 `bit0` 的中点
- 中点最稳，离边界最远，抗抖动/抗噪声最好

在 16 倍过采样下：

`1.5 bit = 1.5 * 16 = 24 tick`

这就是“第 24 个采样 tick”来源。

之后每隔 1 bit（即 16 tick）采样下一个位。

---

## 8. 为什么波特率误差会导致通信失败

UART 无独立时钟线，收发双方各用自己的时钟估计 bit 时间。

如果双方波特率不一致：

- 采样点会随位序号逐步漂移
- 前几位可能正常，后几位偏到边界甚至跨位
- 最终出现乱码、帧错误（`FE`）等

工程上常用经验是总误差尽量控制在约 `±2%` 附近更稳妥（与帧长、采样策略、噪声环境有关）。

---

## 9. 过采样为什么能提升抗干扰

“更快采样”不是为了提高线速，而是为了更可靠判决同一个 bit。

例如 16 倍过采样：

- 一个 bit 内观察多次
- 选中间样本，或做多数投票（具体由实现决定）
- 可抑制短脉冲噪声与边沿抖动影响

要区分两个概念：

- 高波特率：物理上更容易受信号完整性影响
- 高过采样：数字判决更稳（同一波特率下）

---

## 10. STM32 关键寄存器：SR / DR / BRR / CR1

### 10.1 `CR1`（控制）

常见关键位：

- `UE`：USART 总使能
- `TE/RE`：发送/接收使能
- `M`：数据位长度（8/9）
- `PCE/PS`：校验使能与奇偶
- `TXEIE/RXNEIE`：相关中断使能
- `OVER8`：过采样倍率（8 或 16）

### 10.2 `BRR`（波特率）

- 决定分频参数 `USARTDIV`
- 控制内部 tick 节拍精度

### 10.3 `SR`（状态）

- `TXE`：发送数据寄存器空
- `TC`：发送完全完成
- `RXNE`：接收数据就绪
- `ORE`：接收溢出
- `FE`：帧错误

### 10.4 `DR`（数据）

- 写 `DR` 触发发送链路
- 读 `DR` 取接收数据

### 10.5 `RXNE` 中英文与作用（重点）

名称：

- 英文全称：`RXNE (Read Data Register Not Empty / Receive Data Register Not Empty)`
- 中文常译：`接收数据寄存器非空` / `接收寄存器有数据`

含义：

- `RXNE=1`：`DR` 里已经有一字节接收数据，CPU 可以读
- `RXNE=0`：`DR` 里没有新接收数据

用途：

1. 轮询接收：程序一直等 `RXNE=1` 再读 `DR`
2. 中断接收：开 `CR1.RXNEIE`，当 `RXNE=1` 触发中断
3. DMA 接收：硬件在数据就绪时搬运，减少 CPU 参与

注意点：

- 通常读取 `DR` 后，`RXNE` 会被硬件清除（按具体系列手册时序）
- 若新数据到来但旧数据未及时读走，可能触发 `ORE`（溢出）

### 10.6 为什么有 `CR1/CR2/CR3`（控制寄存器分层）

`CR` 是 `Control Register`（控制寄存器）的缩写。  
USART 不是只有一个控制寄存器，而是分成多组，便于扩展与管理：

- `CR1`：核心开关与常用功能（`UE/TE/RE`、字长、校验、中断、过采样）
- `CR2`：帧格式与链路细节（如 `STOP`、同步时钟相关位）
- `CR3`：高级能力（DMA、硬件流控、错误中断等）

为什么要拆分：

- 功能位很多，一个寄存器放不下
- 按层分工后更容易理解与维护
- 新芯片可在后续寄存器扩展，不破坏旧代码风格

### 10.7 硬件状态机运行是什么意思

“硬件状态机（State Machine）”可理解为：  
USART 外设内部有一个自动流程控制器，会按固定状态切换处理接收/发送，不需要 CPU 每个 bit 都手工干预。

接收时它会做这些事：

1. 等待起始位下降沿（`1 -> 0`）
2. 按 `BRR + OVER8` 产生的 tick 计时
3. 在 `1.5 bit` 位置采样 `bit0`
4. 每隔 `1 bit` 采样后续数据位
5. 检查奇偶位（若使能）与停止位
6. 将字节写入 `DR`，置位 `RXNE`
7. 若异常则置位 `FE/ORE` 等错误标志

所以“硬件状态机运行”就是：**外设按预设规则自动走流程，CPU 主要做配置和读结果**。

### 10.8 USART 接收状态图（State Diagram）

下面是一个简化但完整的 UART/USART 接收状态图（8N1，可扩展到校验）：

```text
+------------------+
| IDLE/WAIT_START  |<-------------------------------+
| 线空闲=1         |                                |
+--------+---------+                                |
         | 检测到下降沿(起始位开始)                |
         v                                          |
+------------------+                                |
| START_CONFIRM    |                                |
| 计时到 0.5bit    |                                |
| 再次确认为0      |--否--> 回 IDLE ---------------+
+--------+---------+
         | 是
         v
+------------------+
| ALIGN_TO_BIT0    |
| 再等 1.0bit      |
| 到达 1.5bit 点   |
+--------+---------+
         |
         v
+------------------+      每隔 1bit 采样一次
| DATA_SHIFT       |<-------------------------+
| 采样 bit0..bit7  |                          |
| LSB first 拼字节 |--------------------------+
+--------+---------+
         |
         | 若使能校验
         v
+------------------+
| PARITY_CHECK     |
| 奇偶校验比对     |
+--------+---------+
         |
         v
+------------------+
| STOP_CHECK       |
| 检查停止位=1     |
+---+----------+---+
    |          |
    | 正常     | 异常(FE等)
    v          v
+------------------+      +------------------+
| WRITE_DR_SETFLAG |      | ERROR_FLAG        |
| DR<-接收字节     |      | 置位 FE/ORE/NE... |
| RXNE=1           |      +---------+--------+
+--------+---------+                |
         |                          |
         +------------->------------+
                      回到 IDLE
```

读图方法（建议按这个顺序看）：

1. 从 `IDLE` 开始：等起始位下降沿
2. 进入 `START_CONFIRM`：防抖，避免把噪声当起始位
3. `ALIGN_TO_BIT0`：对齐到 `1.5 bit` 中心采样点
4. `DATA_SHIFT`：逐位采样并组字节
5. `STOP_CHECK`：确认一帧正确结束
6. 正常路径置位 `RXNE`，异常路径置位 `FE/ORE` 等

把它和寄存器对应起来：

- 配置来源：`CR1/CR2/CR3`、`BRR`
- 运行状态：`SR`（`RXNE/FE/ORE/...`）
- 数据出口：`DR`

---

## 11. BRR 为什么是 Mantissa + Fraction（不是纯整数）

因为目标分频通常不是整数，必须支持“小数分频”。

在常见 STM32 设计里（16 倍过采样场景）：

- `Mantissa`：整数部分
- `Fraction`：小数部分（以 `1/16` 为步进）

可理解为“定点小数分频器”，用于降低波特率量化误差。

---

## 12. BRR 的“电路级”实现：整数分频 + 小数累加补偿

这是关键难点，给出一个可实现模型。

### 12.1 模块组成

1. 主计数器（down-counter）：按装载值计数到 0 产生一个 tick
2. 小数累加器 `A`（4 bit）
3. 4 位加法器：`A + Fraction`
4. 进位输出 `carry`（是否溢出 16）
5. 装载值选择：`reload = Mantissa + carry`

### 12.2 工作流程（每次产生 tick 时）

1. 主计数器计到 0，输出一个 tick
2. 执行 `sum = A + Fraction`
3. 若 `sum >= 16`：
  - `carry = 1`
  - `A = sum - 16`
4. 否则：
  - `carry = 0`
  - `A = sum`
5. 下次主计数器装载 `Mantissa + carry`

含义：

- 大多数周期用 `Mantissa`
- 部分周期用 `Mantissa + 1`
- 长期平均分频逼近 `Mantissa + Fraction/16`

这就是“用微小抖动换平均精度”的核心思想。

---

## 13. 例子：手算波特率误差

已知：

- `fCK = 72 MHz`
- 目标 `Baud = 9600`
- `OVER8 = 0`

步骤：

1. `USARTDIV = 72,000,000 / (16 * 9600) = 468.75`
2. `Mantissa = 468`
3. `Fraction = 0.75 * 16 = 12`
4. 实际 `USARTDIV = 468 + 12/16 = 468.75`
5. 反推 `Baud_actual = 72,000,000 / (16 * 468.75) = 9600`

此例量化误差为 0（刚好整齐）。  
但在很多时钟/波特率组合下，`Fraction` 会四舍五入，导致实际波特率有小误差。

---

## 14. FIFO 为什么比单字节 DR 更高级

先给名称：

- 英文全称：`FIFO (First In, First Out)`
- 中文常用名：`先进先出队列` / `先入先出缓冲区` / `FIFO 缓冲`

定义（先理解这个）：

- 谁先写入缓冲区，谁先被读出或发送
- 数据顺序严格保持“先来先走”

无 FIFO（仅 DR）时：

- CPU 需要高频盯 `TXE`
- 高速率下负担重，容易丢节奏

有 FIFO 时：

- CPU 可一次写入多字节
- 硬件排队发送（先进先出）
- 降低 CPU 占用，提高吞吐与稳定性

---

## 15. 最小实用发送模板（轮询版）

```c
static void usart_send_byte(USART_TypeDef *u, uint8_t b)
{
    while (!(u->SR & USART_SR_TXE)) { }
    u->DR = b;
}

void usart_send_buf(USART_TypeDef *u, const uint8_t *buf, size_t n)
{
    for (size_t i = 0; i < n; ++i) {
        usart_send_byte(u, buf[i]);
    }
    while (!(u->SR & USART_SR_TC)) { }  // 等最后停止位发完
}
```

---

## 16. 一页纸记忆版（速查）

- `DR`：数据中转站（CPU 读写入口）
- `TXE`：`DR` 空，可继续写
- `TC`：整帧（含停止位）真正发完
- 移位寄存器：逐位发送执行者
- `BRR`：波特率分频参数（定点小数）
- `OVER8`：8/16 倍过采样选择
- 首次采样 `1.5 bit`：对齐到 `bit0` 中心
- 误差本质：节拍不准 -> 采样漂移 -> 可能跨位
- 过采样价值：提高判决稳健性，不是提高线速

---

## 17. 终极一句话

`CR1` 定规则，`BRR` 定节拍，`SR` 报状态，`DR` 进出数据，移位寄存器与状态机在硬件里自动完成“按位发送/采样判决”。

---

## 18. 结合当前工程的 BRR 实算表（8MHz）

先说明本表使用的工程前提（来自当前代码）：

- `SYSCLK_HZ = 8000000`（见 `include/SYS.h`）
- 当前未看到 RCC 分频重配置代码，可按默认近似：
  - `PCLK1 = 8MHz`
  - `PCLK2 = 8MHz`
- STM32F1 常用 `OVER8=0`（16 倍过采样）计算

因此公式为：

`USARTDIV = PCLK / (16 * Baud)`  
`BRR = (Mantissa << 4) | Fraction`（`Fraction` 取最接近值，范围 0~15）

> 说明：USART1 在 APB2，USART2/3 在 APB1；在本工程当前前提下两者都按 8MHz 计算，结果相同。

### 18.1 常用波特率（PCLK=8MHz, OVER8=0）


| 目标波特率  | USARTDIV(理想) | Mantissa | Fraction(/16) | BRR(十六进制) | 实际波特率(约)  | 误差      |
| ------ | ------------ | -------- | ------------- | --------- | --------- | ------- |
| 9600   | 52.0833      | 52       | 1             | `0x341`   | 9606.15   | +0.064% |
| 19200  | 26.0417      | 26       | 1             | `0x1A1`   | 19184.65  | -0.080% |
| 38400  | 13.0208      | 13       | 0             | `0x0D0`   | 38461.54  | +0.160% |
| 57600  | 8.6806       | 8        | 11            | `0x08B`   | 57553.96  | -0.080% |
| 115200 | 4.3403       | 4        | 5             | `0x045`   | 115942.03 | +0.644% |
| 230400 | 2.1701       | 2        | 3             | `0x023`   | 228571.43 | -0.794% |


### 18.2 快速使用建议

- 若你后续把系统改到 `72MHz`（F103 常见），这张表将不再适用，需要重算。
- 工程实测时，要看“双方总误差”（本机 + 对端），尽量控制在安全范围内。
- 若出现偶发乱码，优先检查：
  - 实际时钟源精度（晶振/RC）
  - 波特率是否与对端完全一致
  - 是否在最后发送后等待了 `TC=1`

### 18.3 可直接拷贝的计算模板

```c
// OVER8 = 0 (16x oversampling)
// PCLK_HZ: 对应 USART 所在总线时钟（USART1=APB2, USART2/3=APB1）
static uint16_t usart_brr_calc(uint32_t pclk_hz, uint32_t baud)
{
    // 四舍五入到最接近的 1/16
    uint32_t div_times_16 = (pclk_hz + (baud / 2U)) / baud; // ~= 16 * USARTDIV
    uint32_t mantissa = div_times_16 / 16U;
    uint32_t fraction = div_times_16 % 16U;
    return (uint16_t)((mantissa << 4U) | fraction);
}
```

---

## 19. USART 寄存器地图（按初始化顺序）

目标是避免常见问题：乱码、首字节异常、中断乱触发、DMA 不生效。

### 19.1 总顺序（一眼版）

1. 开外设时钟（RCC）
2. 配 GPIO 复用功能（TX/RX 引脚）
3. 先关 `UE`（确保改配置时外设不工作）
4. 配 `CR1/CR2/CR3`（帧格式、模式、中断、DMA、流控）
5. 配 `BRR`（波特率）
6. 清状态/清残留标志（必要时读 `SR/DR`）
7. 开 `TE/RE`（收发通路）
8. 最后开 `UE`（正式启动）
9. 若用中断，最后开 NVIC

> 实战建议：把“开 `UE`”放到最后，能减少初始化期间的毛刺与误判。

### 19.2 分步骤寄存器地图（F1 常见）


| 步骤  | 主要寄存器                 | 关键位/动作                           | 作用            |
| --- | --------------------- | -------------------------------- | ------------- |
| 1   | `RCC_APB2ENR/APB1ENR` | 使能 USARTx、GPIOx 时钟               | 外设先有时钟才能访问    |
| 2   | `GPIOx_CRL/CRH`       | TX 设复用推挽，RX 设输入浮空/上拉             | 连接物理引脚功能      |
| 3   | `USARTx_CR1`          | `UE=0`                           | 进入安全配置态       |
| 4   | `USARTx_CR1`          | `M/PCE/PS`、`RXNEIE/TXEIE/TCIE` 等 | 配基础帧格式与中断     |
| 5   | `USARTx_CR2`          | `STOP`（1/0.5/2/1.5）              | 配停止位          |
| 6   | `USARTx_CR3`          | `DMAT/DMAR`、`RTSE/CTSE`、`EIE`    | 配 DMA/流控/高级功能 |
| 7   | `USARTx_BRR`          | 写入波特率分频值                         | 决定 bit 节拍     |
| 8   | `USARTx_SR/DR`        | 视手册建议清残留状态                       | 避免历史标志干扰      |
| 9   | `USARTx_CR1`          | `TE=1`、`RE=1`                    | 打开发送与接收状态机    |
| 10  | `USARTx_CR1`          | `UE=1`                           | USART 正式使能    |
| 11  | `NVIC_ISER`           | 使能 USART IRQ（若使用）                | 允许中断进入内核      |


### 19.3 为什么 `UE` 要最后开

- 避免你改 `BRR/STOP/M` 时外设正在工作
- 避免 RX 线噪声在初始化中被误判为起始位
- 避免中断位已开但参数未全配好导致异常中断
- 对调试最友好：初始化可重复、行为更可预测

### 19.4 最小初始化模板（轮询版，8N1）

```c
// 以 F1 风格为例，省略了具体位定义宏
static void usart_init_polling(USART_TypeDef *u, uint16_t brr)
{
    // 1) RCC: 先开 GPIO/USART 时钟
    // RCC_APB2ENR |= ... ; RCC_APB1ENR |= ... ;

    // 2) GPIO: TX=AF_PP, RX=input floating/pull-up
    // GPIOx_CRL/CRH = ... ;

    // 3) UE=0，进入配置态
    u->CR1 &= ~(1U << 13); // UE

    // 4) CR1: 8bit, no parity, no interrupt
    u->CR1 = 0;            // M=0, PCE=0, TE/RE 先不打开

    // 5) CR2: 1 stop bit
    u->CR2 = 0;

    // 6) CR3: no DMA, no flow control
    u->CR3 = 0;

    // 7) BRR: 波特率
    u->BRR = brr;

    // 8) 可选：读 SR/DR 清历史状态（按系列手册要求）
    (void)u->SR;
    (void)u->DR;

    // 9) 开收发
    u->CR1 |= (1U << 3) | (1U << 2); // TE | RE

    // 10) 最后开 UE
    u->CR1 |= (1U << 13);            // UE
}
```

### 19.5 中断版初始化顺序（在轮询版基础上）

在上面第 9/10 步附近补充：

1. 先配好 `CR1` 对应中断使能位（如 `RXNEIE`）
2. 再 `UE=1`
3. 最后开 NVIC 对应 IRQ

推荐顺序：

`外设先就绪 -> 再放行 NVIC`，可避免刚开 NVIC 就进异常中断。

### 19.6 DMA 版初始化顺序（在轮询版基础上）

1. 配好 USART 基础参数（含 `BRR`）
2. 配 DMA 通道（外设地址、内存地址、长度、方向）
3. 使能 `CR3.DMAT/DMAR`
4. 开 `UE`
5. 启动 DMA 通道

常见坑：只开 DMA 通道但没开 `DMAT/DMAR`，或顺序反了导致首包丢失。